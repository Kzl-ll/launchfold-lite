---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'label inline-flex items-center gap-1 rounded-brand border border-border bg-surface px-3 py-1 text-xs text-muted',
    className,
  ]}
>
  <slot />
</span>
